language: java
dist: xenial
sudo: false
addons:
  apt:
    packages:
      - ant

jdk:
  - openjdk11
  - openjdk13
  - oraclejdk11
  - oraclejdk13

script:
 - "ant -buildfile build.xml clean check jar unittest"
